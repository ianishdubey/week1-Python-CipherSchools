name="Ani"
name = name + "sh" # now we might think how we can change name variable if strings are immutable?
                    #so remember in python we can change value of variable anytime but other methods cannot change our string or we cannot change particular character from string(immutability)

# another way of adding it
name+="sh"
print(name)

age=18
age+=1
print(age)

age*=100
print(age)